<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wallet</title>
    <link rel="stylesheet" href="/css/style.css"> 
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        html, body {
            height: 100%; /* Ensure full height */
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .wallet-balance {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .transactions {
            list-style: none;
            padding: 0;
        }
        .transaction {
            display: flex;
            justify-content: space-between;
            padding: 10px;
            margin: 5px 0;
            border-radius: 5px;
        }
        .credit { background-color: #d4edda; color: #155724; }
        .debit { background-color: #f8d7da; color: #721c24; }
        .breadcrumb {
            display: flex;
            justify-content: center;
            background-color: white;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        .breadcrumb a { text-decoration: none; color: #6e7478; font-weight: bold; }
        .user-options {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
            margin-bottom: 20px;
        }
        .user-option {
            width: 120px;
            padding: 15px;
            background: #fff;
            text-align: center;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s;
        }
        .user-option:hover { transform: scale(1.05); }
        .user-option a { text-decoration: none; color: inherit; font-weight: bold; display: block; }
        footer {
            background-color: #333;
            color: white;
            padding: 15px 0;
            text-align: center;
            margin-top: auto; /* Ensures footer stays at the bottom */
        }
        footer a { color: #fff; margin: 0 10px; text-decoration: none; }
        footer a:hover { color: #ff6600; }
        .main-content {
            flex: 1; /* Pushes the footer down */
        }
    </style>
</head>
<body>
    <nav class="breadcrumb">
        <a href="/">Home</a> - <span>Wallet</span>
    </nav>
    <div class="main-content">
    <div class="user-options">
        <div class="user-option"><a href="/userdetails">My Account</a></div>
        <div class="user-option"><a href="/address">My Address</a></div>
        <div class="user-option"><a href="/add-address">Add Address</a></div>
        <div class="user-option"><a href="/wallet">Wallet</a></div>
        <div class="user-option"><a href="/wishlist">Wish List</a></div>
        <div class="user-option"><a href="/orders">All Orders</a></div>
    </div>
    
    <div class="container">
        <h2>My Wallet</h2>
        <p class="wallet-balance">Balance: ₹<%= wallet.balance %></p>
        <h3>Transaction History</h3>
        <ul class="transactions">
            <% wallet.transactions.forEach(transaction => { %>
                <li class="transaction <%= transaction.transactionType === 'credit' ? 'credit' : 'debit' %>">
                    <span>
                        <i class="icon <%= transaction.transactionType === 'credit' ? 'fas fa-plus-circle' : 'fas fa-minus-circle' %>"></i>
                        <%= transaction.description %>
                    </span>
                    <span>
                        ₹<%= transaction.amount %>
                    </span>
                </li>
            <% }); %>
        </ul>        
    </div>
</div>
    <footer>
        <p>&copy; 2025 Vegishop. All rights reserved.</p>
        <a href="privacy.html">Privacy Policy</a>
        <a href="terms.html">Terms of Service</a>
    </footer>
</body>
</html>
