<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Top Selling <%= type === 'products' ? 'Products' : 'Categories' %></title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        h2 {
            color: #333;
            margin-bottom: 20px;
        }
        table {
            width: 90%;
            border-collapse: collapse;
            background: white;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        th, td {
            padding: 15px;
            border-bottom: 1px solid #ddd;
            text-align: left;
        }
        th {
            background-color: #444;
            color: white;
            text-transform: uppercase;
        }
        tr:hover {
            background-color: #f1f1f1;
        }
        button {
            margin-top: 20px;
            padding: 12px 20px;
            background-color: #444;
            color: white;
            border: none;
            border-radius: 2px;
            cursor: pointer;
            font-size: 16px;
            transition: 0.3s;
        }
        button:hover {
            background-color: #333;
        }
    </style>
</head>
<body>
    <h2>Top 10 Best-Selling <%= type === 'products' ? 'Products' : 'Categories' %></h2>
    
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th><%= type === 'products' ? 'Product Name' : 'Category Name' %></th>
                <th>Total Sold</th>
                <% if (type === "products") { %> <th>Price</th> <% } %>
            </tr>
        </thead>
        <tbody>
            <% data.forEach((item, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td><%= item.name %></td>
                    <td><%= item.totalSales || item.totalQuantity %></td>
                    <% if (type === "products") { %> <td>₹<%= item.price %></td> <% } %>
                </tr>
            <% }) %>
        </tbody>
    </table>

    <button onclick="window.history.back()">Go Back</button>
</body>
</html>
