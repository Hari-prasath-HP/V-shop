<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>V-shop Admin</title>
  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script> <!-- For future enhancements like graphs -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    .btn-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-custom:hover {
      background-color: #d94933;
    }
    .header-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-custom:hover {
      background-color: #d94933; /* Slightly darker shade for hover effect */
    }
    .btn-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-custom:hover {
      background-color: #d94933;
    }
    .header-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-status {
      font-weight: bold;
    }
    .navbar {
      background-color: #343a40;
    }

    .navbar .nav-link {
      color: #f8f9fa;
      font-weight: 500;
      transition: color 0.3s ease;
    }

    .navbar .nav-link.active {
      color: #ffc107;
    }

    .navbar-brand {
      font-weight: bold;
      color: #ffc107;
    }

    .navbar-brand:hover {
      color: #f8f9fa;
    }

    .btn-outline-danger {
      border-radius: 20px;
      font-size: 0.9rem;
    }
    .btn-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-custom:hover {
      background-color: #d94933;
    }
    .header-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-custom:hover {
      background-color: #d94933;
    }
    .header-custom {
      background-color: #eb543b;
      color: white;
    }
    .product-image {
      width: 60px;
      height: 60px;
      object-fit: cover;
    }
    .btn-custom {
      background-color: #eb543b;
      color: white;
    }
    .btn-custom:hover {
      background-color: #d94933;
    }
    .header-custom {
      background-color: #eb543b;
      color: white;
    }
    /* Ensuring image previews are of consistent size */
.preview-container {
  width: 100%;
  max-width: 200px; 
  margin-bottom: 15px;
  text-align: center;
} 

.preview-image {
  width: 100%;
  height: auto;
  max-width: 200px; /* Prevent large images */
}

#image-preview-container {
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
  justify-content: center;
}

/* Optional: Customize the crop button style */
button.btn-primary {
  margin-top: 10px;
}
.error {
    color: red;
    font-size: 0.85em;
  }
        h2, h3 {
            text-align: center;
            color: #333;
        }
        .order-info, .shipping-info, .product-details, .total-amount {
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 8px;
            background: #fafafa;
            box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.1);
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }
        table th {
        color: white;
        background-color: #333 !important;
      }
        th, td {
            padding: 10px;
            text-align: center;
            border-bottom: 1px solid #ddd;
            white-space: nowrap;
            text-overflow: ellipsis;
            overflow: hidden;
        }
        .total-amount h3 {
            color: #007bff;
            text-align: center;
        }
        .product-image {
            width: 50px;
            height: 50px;
            object-fit: cover;
            border-radius: 5px;
        }
        .status-dropdown {
            padding: 5px;
            border-radius: 5px;
        }
        .product-img {
            width: 80px; /* Adjust width as needed */
            height: 80px; /* Adjust height as needed */
            object-fit: cover; /* Ensures image fills the box without distortion */
            border-radius: 8px; /* Adds smooth rounded corners */
            box-shadow: 2px 2px 8px rgba(0, 0, 0, 0.1); /* Optional shadow for better appearance */
            display: block; /* Ensures proper alignment */
            margin: auto; /* Centers the image inside the table cell */
        }
        h2 {
            text-align: center;
            color: #333;
        }
        th, td {
            padding: 12px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }
        .status-dropdown {
            padding: 5px;
            border: 1px solid #777;
            background: #222;
            color: white;
        }
        .view-btn {
            background: #28a745;
            color: white;
            padding: 8px 12px;
            border: none;
            cursor: pointer;
        }
        .view-btn:hover {
            background: #218838;
        }
        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 20px;
        }
        .pagination a {
            margin: 0 5px;
            padding: 8px 12px;
            text-decoration: none;
            background: #444;
            color: white;
        }
        .pagination a:hover {
            background: #333;
        }
        .table th, .table td {
            text-align: center;
            vertical-align: middle;
        }
        .status-active {
            color: green;
            font-weight: bold;
        }
        .status-inactive {
            color: red;
            font-weight: bold;
        }
        .table{
            width: 100%;
            table-layout: fixed;
            margin: auto;
        }
        thead th {
            width: auto;
        }
        .table-responsive {
        overflow-x: auto; 
        max-width: 100%;
      }
      .title {
            text-align: center;
            color: #333;
        }
        .report-actions {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-bottom: 20px;
        }
        .download-xlsx { background: #28a745; }
        .download-pdf { background: #dc3545; }
        .filters {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 20px;
        }
        .filter-row {
            display: flex;
            gap: 15px;
            align-items: center;
            margin-bottom: 10px;
            flex-wrap: wrap;
            justify-content: center;
        }
        .filters label {
            font-weight: bold;
        }
        .filters input, select {
            padding: 10px;
            width: 200px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .sales-table {
            width: 100%;
            table-layout: fixed; /* Ensures the table fits within its container */
            border-collapse: collapse;
            margin-top: 20px;
            background: white;
            border-radius: 10px;
            overflow: hidden;
        }

        .sales-table th, .sales-table td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: center;
            word-wrap: break-word; /* Ensures long text wraps within the cell */
            white-space: normal; /* Allows text to wrap instead of overflowing */
        }

        .sales-table th {
            background-color: #333;
            color: white;
        }

        .sales-table tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        /* Responsive Table Adjustments */
        @media screen and (max-width: 1024px) {
            .sales-table th, .sales-table td {
                font-size: 14px; /* Reduce font size on smaller screens */
                padding: 8px;
            }
        }

        @media screen and (max-width: 768px) {
            .sales-table th, .sales-table td {
                font-size: 12px; /* Further reduce font size */
                padding: 6px;
            }
            .sales-table {
                display: block;
                width: 100%;
            }
        }

        .status {
            font-weight: bold;
            padding: 5px;
            border-radius: 5px;
        }
        .status.pending { color: orange; font-weight: bold; }
        .status.completed { color: green; font-weight: bold; }
        .status.canceled { color: red; font-weight: bold; }

        .table-container {
    width: 95%;
    overflow-x: auto;
    margin: 0 auto;
    min-height: 100vh; /* Optional: Centers vertically */
}

  </style>
    <!-- Toastify CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">  
</head>
<body class="bg-light">
    
